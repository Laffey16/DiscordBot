[project]
name = "discordbot"
version = "0.1.0"
description = "Discord Bot with various commands for portfolio and learning purposes."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "py-cord>=2.8.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
